services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: foodbridge
      MYSQL_USER: foodbridge
      MYSQL_PASSWORD: foodbridge
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
